<%= form_for(@socialmedium) do |f| %>
  <% if @socialmedium.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@socialmedium.errors.count, "error") %> prohibited this socialmedium from being saved:</h2>

      <ul>
      <% @socialmedium.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :username %><br>
    <%= f.text_field :username %>
  </div>
  <div class="field">
    <%= f.label :type %><br>
    <%= f.text_field :type %>
  </div>
  <div class="field">
    <%= f.label :contact_id %><br>
    <%= f.number_field :contact_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
